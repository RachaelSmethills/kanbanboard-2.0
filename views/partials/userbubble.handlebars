<div class="flex-item">
    <div class="thumbnail text-center">
        <img src="{{user.avatarUrl}}" alt="" class="img-responsive">
        <div class="caption">
            <p>{{user.name}}</p>
        </div>
    </div>
</div>

